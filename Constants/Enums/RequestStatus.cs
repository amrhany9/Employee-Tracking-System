namespace back_end.Constants.Enums
{
    public enum RequestStatus
    {
        Pending = 1,      // The request is awaiting approval.
        Approved = 2,     // The request has been approved.
        Declined = 3,     // The request has been declined.
    }
}
